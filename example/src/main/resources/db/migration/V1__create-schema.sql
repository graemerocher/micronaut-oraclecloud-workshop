CREATE TABLE owner (id BIGINT AUTO_INCREMENT PRIMARY KEY, name VARCHAR(255) NOT NULL, age INT NOT NULL);
CREATE TABLE pet (id BIGINT AUTO_INCREMENT PRIMARY KEY, name VARCHAR(255) NOT NULL, health VARCHAR(18) NOT NULL, owner_id BIGINT NOT NULL);